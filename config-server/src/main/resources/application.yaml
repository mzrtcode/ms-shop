server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///home/mzrt/ms-shop/config-data
          clone-on-start: true
          default-label: main